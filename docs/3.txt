  P0 立即项（统一体验与导航）

  - 版心与留白统一自查
      - 检查全站使用 .container 的页面，确保已用 display:block; padding:0 16rpx; 或 .content { padding:0 16rpx; } 统一版心，避免全
  局 .container flex 带来的“被压窄”问题。
      - 点名再核：知识（disease-list/detail）、社区列表（qa-list/share-list）、文章详情（article-detail）、发布表单（qa-post/share-
  post）、疫情页（epidemic/index）。
  - 社区入口与回路
      - “我的”页增加社区入口：蜂农提问、经验分享（指向列表页，右下悬浮发布）。
      - 首页“防控知识”已跳百科分类；再确认 Tab 切换、返回路径是否符合预期。
  - 细节统一（已做基础）
      - 列表卡片、详情卡片、表单卡片的圆角、阴影、上下间距与字体层级统一再过一遍（与检测页保持一致）。

  P1 重要项（完成社区与百科的最小闭环）

  - 蜂病百科
      - 分类列表：右侧“X篇”明确指“管理员发布文章数”，与详情页文章数量一致。
      - 详情页：信息卡已加“分类介绍”徽章；文章区块保留 3 篇，不再有“查看全部”。
      - 文章详情：正文展示 + “相关文章”区块（已做），再微调排版与行距。
  - 社区列表（提问/分享）
      - 搜索栏与话题 Chips 仅做 UI；不做真实检索，保持禁用态占位。
      - 卡片结构按“头像/昵称/标签/日期/右侧轻统计 + 正文浅灰泡泡”统一（已做）。
  - 社区详情
      - 提问详情：帖子卡片 + 建议卡片（有则显），回复列表卡片化；帖子卡内右下“回复N”，删除中间重复标题（已做）。
      - 分享详情：帖子卡片 + 审核提示已去除；与提问详情版心一致。

  P2 可选项（增强易用与稳定）

  - 安全区适配与悬浮按钮
      - FAB（＋）与底部回复栏适配 iOS 安全区：底部留白适配（可使用 constant(safe-area-inset-bottom) 或按平台判断增加 8–16px）。
  - 状态/空态
      - 列表与详情的空态提示（“暂无内容”）与加载状态占位骨架。
  - 行为一致性
      - Chips 高亮与病害参数联动：从百科详情跳到提问/分享列表时，Chips 高亮对应病害（仅 UI 高亮，不做数据筛选）。
  - 视觉微调（如需）
      - 文章条目卡阴影强度、卡间距（12rpx→10rpx）、标题字号（28/30rpx）根据你的偏好再细化。

  疫情模块（按原型先跑通，暂不深入）

  - 图表已可用；若后续需要再做：
      - 省/市/县级联与数量/占比切换
      - iOS/Android 2D Canvas 兼容再验证与安全边距

  后端对接前的约定（占位）

  - 不跑通发布与审核流，只保留发布表单 UI。
  - 文章数据暂用本地 mock，后端完成后将 ARTICLE 抽到 services/community.js，统一替换。



  建议导航结构与跳转

  - 检测服务
      - 我的检测记录 → 跳转 /pages/results/list/index（替换“预警记录”）
      - 自费检测与审核 → 跳转 /pages/detection/index 且默认激活“自费检测”分段（替换“资讯浏览记录”）
      - 邮寄指南 → 跳转 /pages/detection/guide/index（保留常用入口，减少用户迷路）
  - 社区与知识
      - 我的话题发布/回复 → 保留 /pages/profile/topicRecord/index
      - 知识库与社区 → 跳转 /packageCommunity/pages/disease-list/index（统一三标签入口）
  - 疫情可视化
      - 疫情地图/趋势 → 跳转 /packageCharts/pages/epidemic-map/index 或 /packageCharts/pages/epidemic-trend/index
  - 账户与设置
      - 账号与绑定 → 若未登录跳转 /pages/auth/login/index；已登录展示手机号并提示“更换手机号”（占位）
      - 消息通知授权 → 保留现有授权占位
      - 帮助与客服 → 弹出电话/复制客服微信（现有方法可复用）
      - 用户协议/隐私政策 → 使用本地 WebView 占位或 Toast 提示

  UI/交互小优化

  - 顶部状态卡片
      - 未登录显示“微信一键登录”按钮（跳 /pages/auth/login/index）
      - 已登录显示手机号与“查看检测记录”快捷按钮
  - 最近检测概览（可选，小卡片）
      - 使用 resultsSvc.getResults({ page:1, pageSize:1 }) 拉取最近一条，展示状态与快速入口（详情/进度）
      - 纯前端 Mock，不改服务层
  - 统一视觉
      - 使用全局按钮类：u-btn u-btn-primary u-btn--sm/--md，按 docs/UI-Palette-and-Usage.md

  最小实施方案（不新增页面）

  - 文案替换与路由调整（优先级高）
      - 将“预警记录”改为“我的检测记录”，点按跳 /pages/results/list/index
      - 将“资讯浏览记录”改为“自费检测与审核”，点按跳 /pages/detection/index 并在页面内切换到“自费检测”分段（可通过 query 或在检测页
  上次模式记忆实现）
      - 新增“邮寄指南”项，点按跳 /pages/detection/guide/index
      - 新增“知识库与社区”，点按跳 /packageCommunity/pages/disease-list/index
      - 保留“话题发布/回复”“消息授权”“帮助与客服”
  - 登录态提示
      - 未登录在“我的检测记录”点击时先跳 /pages/auth/login/index

  可选后续（按需）

  - 新增“我的地址管理”页（Mock），管理邮寄信息（符合 P0 邮寄流程增强）
  - 新增“我的检测号/赠送码”页（Mock），便于查找来源编号
  - 新增“认证”入口（占位），满足“结果查询与认证页面”在主包的导航要求

  请确认你要执行的范围：

  - 仅替换文案与点击路由（最快）
  - 另加“最近检测概览”小卡片
  - 继续新增“邮寄地址管理/检测号”占位页（Mock）后挂到“我的”页

  我可以先做第一步（文字与路由替换），验证通过后再逐项补强。