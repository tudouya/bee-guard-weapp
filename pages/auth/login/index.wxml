<view class="page">
  <view class="top">
    <view class="logo-wrap">
      <view class="logo">
        <image class="logo-img" src="/images/profile-avatar-default.png" mode="aspectFill"></image>
      </view>
    </view>
    <text class="app-name">蜜蜂疫病监控平台</text>
    <text class="subtitle">使用微信手机号一键登录，查看您的检测记录</text>
  </view>

  <view class="actions">
    <button 
      class="u-btn u-btn-primary u-btn--md u-btn--pill u-btn-block login-btn"
      open-type="{{agree ? 'getPhoneNumber' : ''}}"
      bindgetphonenumber="onGetPhoneNumber"
      bindtap="onLoginTap"
      loading="{{loading}}"
      disabled="{{loading}}"
    >微信一键登录</button>

    <view class="hint">请先勾选下方《用户协议》和《隐私政策》，再点击登录</view>
  </view>

  <view class="footer">
    <checkbox-group bindchange="onAgreeChange">
      <label class="agree-row">
        <checkbox value="agree" checked="{{agree}}"/>
        <text class="agree-text">登录即表示同意</text>
        <text class="link" bindtap="openUserAgreement">《用户协议》</text>
        <text class="agree-text">和</text>
        <text class="link" bindtap="openPrivacy">《隐私政策》</text>
      </label>
    </checkbox-group>
  </view>
</view>
